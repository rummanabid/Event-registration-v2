<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Analytics — Events QR Check-in</title>
  <link rel="stylesheet" href="/css/style.css" />
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.2/dist/chart.umd.min.js"></script>
</head>
<body>
  <header class="site-header">
    <div class="header-inner">
      <a href="/" class="logo">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/>
          <rect x="14" y="14" width="7" height="7"/><rect x="3" y="14" width="4" height="4"/>
          <rect x="9" y="14" width="4" height="4"/>
        </svg>
        Events QR Check-in
      </a>
      <nav class="site-nav">
        <a href="/">Home</a>
        <a href="/admin">Admin</a>
        <a href="/analytics" class="active">Analytics</a>
        <a href="/scan">Scanner</a>
      </nav>
    </div>
  </header>

  <main class="analytics-page">
    <div class="container">
      <div class="page-header">
        <div>
          <h1>Analytics</h1>
          <p class="text-muted" id="analytics-subtitle">Registration and attendance overview</p>
        </div>
        <div style="display:flex;gap:10px;align-items:center">
          <% if (events.length > 1) { %>
          <select id="event-filter" onchange="onEventFilter(this.value)" style="padding:8px 12px;border:1px solid #e2e8f0;border-radius:8px;font-size:.9rem;background:#fff;cursor:pointer">
            <option value="">All Events</option>
            <% events.forEach(e => { %>
            <option value="<%= e.id %>"><%= e.name %></option>
            <% }) %>
          </select>
          <% } %>
          <a href="/admin" class="btn btn-outline">← Admin</a>
        </div>
      </div>

      <!-- KPI Cards -->
      <div class="kpi-grid" id="kpi-grid">
        <div class="kpi-card kpi-loading">Loading...</div>
        <div class="kpi-card kpi-loading">Loading...</div>
        <div class="kpi-card kpi-loading">Loading...</div>
        <div class="kpi-card kpi-loading">Loading...</div>
      </div>

      <!-- Charts -->
      <div class="charts-grid">
        <div class="card chart-card">
          <div class="card-header"><h2 id="reg-chart-title">Registrations — Last 14 Days</h2></div>
          <div class="card-body">
            <canvas id="registrations-chart" height="120"></canvas>
          </div>
        </div>
        <div class="card chart-card">
          <div class="card-header"><h2 id="donut-chart-title">Overall Check-in Rate</h2></div>
          <div class="card-body chart-body-center">
            <canvas id="checkin-rate-chart" height="180"></canvas>
            <div class="chart-center-label" id="checkin-rate-label"></div>
          </div>
        </div>
      </div>

      <!-- Per-event table -->
      <div class="card">
        <div class="card-header"><h2 id="event-table-title">Event Breakdown</h2></div>
        <div class="card-body">
          <div id="event-table-container">
            <div class="empty-state"><p>Loading...</p></div>
          </div>
        </div>
      </div>
    </div>
  </main>

  <footer class="site-footer">
    <p>Events QR Check-in &copy; <%= new Date().getFullYear() %></p>
  </footer>

  <script src="/js/analytics.js"></script>
</body>
</html>
